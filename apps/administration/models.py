"""
Administration app models.
(Currently empty - all admin functionality uses existing models from other apps)
"""

# Admin models are managed via the API endpoints
# See: apps.administration.viewsets and apps.administration.serializers
