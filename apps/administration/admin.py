"""
Administration app Django admin.
(Not used - all admin functionality exposed via API at /api/admin/)
"""

from django.contrib import admin

# Admin models are managed via the API, not the Django admin interface
